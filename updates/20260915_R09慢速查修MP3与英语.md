# R09 慢速查修：MP3 下限 300s + 英语卡不泄答案（2026-09-15）

用户指出：上一轮太快；**300 秒是下限**（体育课长听），不是上限。

## 必改

| 问题 | 原状 | 现在 |
|------|------|------|
| MP3 时长 | 90–300s，300 被当上限 | **min=300s / max=900s** |
| 讲解稿长度 | 800–1500 字≈2–4 分 | **1800–3500 字**（生成+改写） |
| TTS | `input[:2000]` 截断，永远到不了 300s | **按句分段合成再拼 MP3** |
| 夜间 TTS | `body[:1500]` | 用全文 |
| 上传转 MP3 | `text[:1500]` | 用全文（内部再切） |
| 英语卡 | JSON 里直接带 `answer` | **卡面无 answer**；`/english/reveal` 揭示 |

## 检修方式

- 通读 `media_service` / `english_vocab` / `review_service` / `generator`  
- 不是只跑冒烟：抓到 TTS 截断与答案泄露两个真 bug  
- `check_imports` ERRORS=0  
- 单测 **25 passed**（english + review + material + offline + r03 + multiuser）

## 配置

```json
"material_mp3_min_sec": 300,
"material_mp3_max_sec": 900
```
