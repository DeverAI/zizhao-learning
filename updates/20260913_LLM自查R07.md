# LLM 代码自查 R07（2026-09-13）——三路通读，不是冒烟

方式：并行审查 auth/security、material/review、media/models + 主审复核。  
标准：只记真缺陷；修完 `check_imports` ERRORS=0 + 全量单测。

## 已核验并修复

### P0
| 问题 | 修复 |
|------|------|
| `demote_old_active(user_id="")` 全表 demote 所有用户 | 强制 `WHERE user_id=?` |
| PassKey 登录 challenge 可重放 | `begin_passkey/login/begin` 一次性 challenge，消费后作废 |
| `/review/run` 无归属、可改他人正文 | 校验 `mat.user_id`；已 passed 跳过 |
| `GET /audio/{id}` 无归属/无审核门控 | 归属 + `audio_allowed`，未过审返回空 |
| session 前缀碰撞 `alice`→`alice2` | 必须 `uid` 或 `uid:` 前缀 |
| `upload` 背诵路径 `STORAGE_DIR` NameError | late import |
| `generate_from_plan` 未传 user Key | 贯通 `user_id` |

### P1
| 问题 | 修复 |
|------|------|
| X-Forwarded-For 伪造绕过限流 | 直连只用 `client.host` |
| revoke 设备后会话仍活 | `delete_sessions_for_device` |
| OCR 失败仍可能 status=ready | `ready = not degraded and text` |
| 小米搜索同步 Client 堵事件循环 | `AsyncClient` |
| 末轮改写不复审 | 末轮不改写 |
| `coach_review` 无 user 隔离 | 传 `user_id` |
| `chat_system_prompt` 记忆无 user | 传 `material.user_id` |

## 已知未改（记录）
- `material_plan` 仍全局池（三人共享大纲条目）——产品选择，非崩溃  
- archive `hard_key` 全局唯一——可能跨人挡同题；下轮可加 user 维度  
- PassKey 仍是简化 HMAC，非完整 WebAuthn  
- `SESSION_SECRET` 默认值需部署时用环境变量覆盖  

## 验证
`check_imports` ERRORS=0 · unittest **42 passed**
