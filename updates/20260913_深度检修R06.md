# R06 循环记录（2026-09-13）——修完再查，不以一次通过收工

## 事故

本地工作区 `backend/` 一度被删（git status 全 D）。  
**处置**：立即停手 → `git checkout HEAD -- backend`（8ba3ae7）恢复 → 未做破坏性命令行回滚。

## 循环1 已修（上一轮）

- 锁跨 await 死锁  
- refresh 串号  
- 审核 clean 逻辑 / TTS 阻塞 / late path  

## 循环2（本轮再查）

| 问题 | 证据 | 修复 |
|------|------|------|
| **P0 部署崩溃** | 服务器 uvicorn：`material.py` `Depends` 未导入 | 补 `from fastapi import ... Depends` |
| P1 路径绑定 | persona/recitation/upload/review 模块级 `STORAGE_DIR` | 函数内 late import |
| 探针误报 | `app.routes` 显示 `_IncludedRouter` | 改为 `/openapi.json` 校验 |

## 循环3 验证（本轮干净标准）

- `check_imports.py`：模块全 OK，关键路由 OK， **ERRORS=0**  
- 单测 **42 passed**  
- 服务器需重新部署后再验 `/api/system/health`

## 流程纪律（写给自己）

1. 修完 ≠ 对：必须 `check_imports` + 全量 unittest  
2. 部署后必须 **远程 health**，不能只看本机  
3. 仍有 FAIL → 不收工，再开一轮  
