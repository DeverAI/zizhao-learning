# 深度检修 R06（2026-09-13）——字面通读，非冒烟

> 方法：读 `material_service` / `agent_chat` / `review_service` / `user_api_service`  
> AST/源码探针 `audit_probe_r06.py` → 修复 → 钉测  
> 备份：`backups/audit_r06_20260913/`

## 抓到并修掉的真问题

| 级别 | 编号 | 问题 | 为何严重 | 修复 |
|------|------|------|----------|------|
| P0 | LOCK-01/02 | `threading.Lock` 包住 `await` 生成 HTTP | **事件循环死锁**：一人生成时全站卡死 | 生成在锁外；仅写库双检短锁 |
| P0 | ISO-01 | `agent_chat` 里 `refresh_material` 未传 `user_id` | 换素材写到空用户，串号 | `user_id=user_id` |
| P1 | PATH-01 | `user_api._path` 绑定导入时 STORAGE | 测试隔离/热切换失效 | late import |
| P1 | CLEAN-01 | clean 条件把 LLM 分数与 issues 绕在一起 | 可能「有刺却 clean」 | 有刺必 False；分步判定 |
| P1 | SCORE | `int(score)` 非数字崩溃 | 审核中断 | try/except |
| P1 | TTS | 审核后同步 TTS 堵事件循环 | 夜间卡死 | `asyncio.to_thread` |

## 探针结果（修后）

LOCK-01 OK · LOCK-02 OK · ISO-01 OK · CLEAN-01 OK · PATH-01 OK  
回归 **26 passed**

## 部署状态

- GitHub：已推 AGPL（脱敏）  
- 服务器 mindog：见下一节部署脚本/目录 `C:\all_projects\zizhao-learning`  

## 仍接受风险

- user `base_url` 可指向内网（三人自用可接受；公网须加白名单）  
- Cookie 未标 `Secure`（本地 HTTP；上 HTTPS 后应加）  
- 审核 LLM 与生成共用用户 Key，配额共用  
