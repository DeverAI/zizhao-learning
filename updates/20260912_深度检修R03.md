# 深度检修 R03（2026-09-12）

> 场景锚点：初三补漏 + 自招；平台已含鉴权/组件/上传  
> 备份：`backups/audit_r03_20260912/`  
> 探针：`backend/audit_probe_r03.py`  
> 回归：`test_r03_fixes` + 既有套件 → **30 passed**

## 方法

通读鉴权/安全/媒体/Agent/路由/前端 → 探针复现 → 备份 → 修复 → 钉测试 → 本文档。

## 发现与处理

### P0（已修）

| 编号 | 问题 | 修复 |
|------|------|------|
| AUTH-01 | `login_with_passkey` 不校验签名，伪造 signature 可登录 | signature 必须 = HMAC-SHA256(public_key, challenge) |
| AUTH-02 | `current_user` 接受裸 sid，绕过 Cookie HMAC | **仅**接受 `sid.sig` |

### P1（已修）

| 编号 | 问题 | 修复 |
|------|------|------|
| PATH-01 | media 模块级绑定存储路径 | 函数内 late import；user_id 目录消毒 |
| AGENT-01 | 对话工具要 user_id 但未注入 | chat 路由读 Cookie；agent_chat 自动注入 + system 注入常驻/时间表 |
| RL-01 | default 限流 120/min 误伤前端 | 提到 600/min |
| FEAT-01 | 无小测→错题→补漏闭环 | `quiz_service` + `/api/quiz/*` + 前端自招页面板 |
| FEAT-02 | 常驻资料未进对话上下文 | system prompt 注入 resident/timetable |

### P2

| 编号 | 问题 | 处理 |
|------|------|------|
| API-01 | multipart bool | 既有 Form(bool) 可解析；上传链路测试覆盖 |

## 初三功能线索评估（R03 后）

| 能力 | 状态 |
|------|------|
| 每日素材 + 找茬 | 有 |
| **小测→错题→gap_fill 计划** | **本轮闭环** |
| 时间表/常驻资料 + Agent 可写 | 有（登录态） |
| 上传 OCR/MP3 | 有，失败 degraded |
| ESP PassKey | 有；Web PassKey 为简化 HMAC |
| 大纲内容量 | 仍少（种子+课程自招） |
| 真 WebAuthn | 未做（文档标明） |

## 下一轮建议

1. 哲学/历史/古诗文大纲扩充（≥30/域）  
2. 完整 WebAuthn 替换简化 PassKey  
3. 小测成绩曲线 + 按薄弱点自动改 pick 策略  
4. ESP 固件示例（device/login + 拉今日 + 播 MP3）  

## 回滚

复制 `backups/audit_r03_20260912/backend/*` 回 `backend/`。

## 接受风险

- 简化 PassKey 非完整 WebAuthn  
- OCR/TTS 依赖本机能力与密钥  
- 限流进程内内存，多 worker 不共享  
