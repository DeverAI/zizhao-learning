# 自招素材系统 技术方案

> 日期：2026-09-10 ｜ 作者：Mavis ｜ 状态：技术方案稿（待确认后实施）
> 目标场景：上海中考自主招生（初中升高中，2027 年中考）
> 内容范围：哲学 / 历史 / 高中古诗文（含特殊句式）
> 说明：本方案只定**技术架构与机制**，不含素材大纲内容（按用户要求后置）
>
> 前序：ESP 摄像头终端方案已封存，见 [hardware/20260910-Mavis-ESP语音视觉终端方案.md](hardware/20260910-Mavis-ESP语音视觉终端方案.md)

## 0. 设计要点速览

| # | 决策 | 一句话理由 |
|---|------|-----------|
| 1 | **素材分三态：active / recent / archive** | 归档不是删数据，是**降维成指纹**，才可能"防止 AI 出重复内容" |
| 2 | **归档指纹要小到能全量塞进 prompt** | 每条 100-200 字节，一年 365 条约 70KB → 全年已用清单可作负例 |
| 3 | **大纲数据化（material_plan 表）** | 把"顺序"变成数据，AI 只在计划条目范围内发挥，否则每天素材随机漂移 |
| 4 | **去重三层：硬键 / 关键词相似 / prompt 负例** | 只靠模型约束必然漏，硬索引是底线 |
| 5 | **生成幂等** | 照抄现有 `daily_quote` 已验证写法：今日文件存在即复用 |
| 6 | **对话挂载素材**（session 绑定 material_id） | "整天对话依赖它"落到实现就是会话持有素材引用 |
| 7 | **音频预生成** | "开机就能听"要求开机即播，不能等 TTS 实时合成 |
| 8 | **归档挂夜间巡检** | 复用项目已有 `night_patrol`（01:00-05:00），不新增调度器 |
| 9 | **设备固件自研，不用小智生态** | 用户明确「不要小智」（2026-09-10）。代价：自己写 WiFi/HTTP/I2S/按键；收益：不受小智板卡约束，且我们的音频链路本就更简单（§11.2） |
| 10 | **外放场景 → 双麦 + 硬件 AEC 成为必需** | 外放 + 麦克风同时工作必然回声反噬；单麦板不可用（§11.5） |
| 11 | **屏幕必需，且要能显示文字** | 用户明确「轻巧可以，不能没有，需要文字」（2026-09-10）。推翻了 §11.3 的「无屏」设想 |
| 12 | **倾向墨水屏** | 「长待机 + 需要文字」这组约束指向墨水屏：断电保持显示、环境光可读、静态零功耗（§11.10） |
| 13 | **断点续播 → 音频分段 + 服务端记进度** | 长素材不可能一次听完；分段还能兼得「边下边播」降低首播延迟（§11.11） |

## 1. 需求还原

用户原话拆解（逐条对应到机制）：

| 需求 | 落到哪个机制 |
|------|--------------|
| 用自己的 API 的 AI 找自招能用的素材 | §7 生成流程；复用现有 `zhipuai_search` / `kimi_search` 联网 + 多模型 |
| 哲学、历史、高中古诗文篇目特殊句 | §9 计划表的 `domain` 维度 |
| 能多轮对话交流 | §8 素材挂载式对话 |
| 每天开机后若未准备新的则准备一个素材先 | §7 幂等生成（`GET /api/material/today`） |
| 整天对话依赖它 | §8 会话绑定 `material_id` |
| 可以对话要求查新的 | §8.2 工具调用 `refresh_material` |
| 新的过期后自动压缩存储 | §4 三态 + §5 归档降维 |
| 防止 AI 搞相同的 | §6 去重三层 |
| 提前拟定顺序 & 大纲 | §9 `material_plan` 表（本次只定结构，不填内容） |
| 很小巧，吃饭玩玩听听 | §11 精简设备只消费两个接口 |

## 2. 现状盘点：可以直接吃的能力

本项目的基建比预期厚，这个系统大半是在**复用**而不是新建。

**已可复用的模型与能力**（`backend/services/ai_service.py`）

| 能力 | 现成方法 | 用途 |
|------|----------|------|
| 联网搜索 | `zhipuai_search(query)`、`kimi_search(query)` | 找素材原始资料 |
| 结构化生成 | `deepseek_json(...)`、`_chat_json(...)` | 生成素材 JSON |
| 轻任务 | `light_task_chat(messages, max_tokens)` | 摘要、指纹抽取 |
| 多轮 Agent | `agent_multi_turn(messages, max_rounds)` | 多轮对话 |
| 带工具的对话 | `_chat_with_tools(...)` | "查新的"工具调用 |
| TTS | `xiaomi_tts(text, voice)` | 素材朗读（预生成音频） |
| 多模型可切 | DeepSeek / Kimi / GLM / MiMo / Qwen（settings 已配 5 家） | 按任务选便宜或强的模型 |

**已可复用的工程模式**

| 模式 | 现成位置 | 本次对应 |
|------|----------|----------|
| **每日幂等缓存** | `routers/system.py` 的 `daily_quote`：`storage/quotes/quote_YYYY-MM-DD.json`，存在即复用 | 今日素材生成，**照抄这个模式** |
| **原子写 JSON** | `_atomic_write_json` | 素材与归档落盘 |
| **记忆衰减** | `hippocampus_service.py`：`_compute_decay` / `FADE_THRESHOLD` / `FORGET_THRESHOLD` | 素材"过期"判定，**同一套思路** |
| **冲突检测** | `hippocampus_service.detect_conflict(topic, new_knowledge)` | 去重 L2 的参考实现 |
| **周期任务** | `night_patrol`（01:00-05:00） | 归档压缩挂在这里 |
| **神经开关** | `config.py` 的 `ENABLE_*` 系列 | `ENABLE_MATERIAL_SYSTEM` |
| **多态配置** | `SavedConfig{config_type, name, payload}` | 计划表的轻量备选 |
| **异步 ORM + 自动迁移** | `models/database.py`：`create_all` + `_migrate_table_sync` | 新表迁移沿用同款 |

**结论：不需要引入任何新依赖。** 没有 embedding、没有向量库、没有新调度器——去重和归档全部用纯 Python + SQLite 实现。

## 3. 数据模型

三张表（`models/models.py` 新增，`database.py` 的迁移清单同步追加）。

### 3.1 `material_plan` — 大纲（计划表）

```python
class MaterialPlan(Base):
    __tablename__ = "material_plan"
    id            = Column(String, primary_key=True, default=gen_id)
    domain        = Column(String, index=True)   # philosophy / history / classics
    seq           = Column(Integer, index=True)  # 顺序权重
    title         = Column(String)               # 计划条目名，如「《劝学》宾语前置」
    source_hint   = Column(Text, default="")     # 指定出处/检索关键词（可空，交 AI 补全）
    tags          = Column(JSON, default=list)   # 主题标签，用于去重与轮换
    status        = Column(String, default="pending")  # pending/used/skipped/blocked
    used_at       = Column(DateTime, nullable=True)
    material_id   = Column(String, default="")   # 实际生成出的素材
    note          = Column(Text, default="")     # 人工备注 / 反馈原因
```

**为什么必须有这张表**：用户要的"顺序 & 大纲"如果只写进 prompt，模型每天自由发挥，一个月后素材必然漂移、重复、忽难忽易。**把大纲变成数据，AI 只在被选中的那一条上做"展开"，不做"选题"。**

### 3.2 `materials` — 素材（今日 + 近期）

```python
class Material(Base):
    __tablename__ = "materials"
    id            = Column(String, primary_key=True, default=gen_id)
    plan_id       = Column(String, default="")
    domain        = Column(String, index=True)
    title         = Column(String)
    source        = Column(Text, default="")     # 出处/来源标注
    body          = Column(Text, default="")     # 可朗读正文（讲解稿）
    key_points    = Column(JSON, default=list)   # 关键点，供对话追溯
    followups     = Column(JSON, default=list)   # 追问种子（多轮对话的火种）
    concept_keys  = Column(JSON, default=list)   # 概念关键词集合 → 去重 L2 的输入
    fingerprint   = Column(JSON, default=dict)   # {person, work, concept, era...} 结构化指纹
    audio_path    = Column(String, default="")   # 预生成音频
    status        = Column(String, default="active")  # active/recent/archived/failed
    feedback      = Column(String, default="")   # up/down，回写计划表权重
    day_key       = Column(String, index=True)   # YYYY-MM-DD（北京时间），幂等键
    created_at    = Column(DateTime, default=_utcnow)
    used_at       = Column(DateTime, nullable=True)
```

### 3.3 `material_archive` — 归档（降维指纹）

```python
class MaterialArchive(Base):
    __tablename__ = "material_archive"
    id            = Column(String, primary_key=True, default=gen_id)
    material_id   = Column(String, index=True)
    domain        = Column(String, index=True)
    title         = Column(String)
    source        = Column(Text, default="")
    concept_keys  = Column(JSON, default=list)
    fingerprint   = Column(JSON, default=dict)
    one_line      = Column(Text, default="")     # 一句话摘要
    hard_key      = Column(String, unique=True, index=True)  # 去重 L1 的硬键
    used_day      = Column(String, index=True)
    archived_at   = Column(DateTime, default=_utcnow)
```

**注意：archive 里没有 `body`。** 正文不进归档，这是"压缩"的核心动作。

### 3.4 `material_progress` — 播放进度（断点续播）

```python
class MaterialProgress(Base):
    __tablename__ = "material_progress"
    id            = Column(String, primary_key=True, default=gen_id)
    material_id   = Column(String, index=True)
    segment_index = Column(Integer, default=0)   # 当前段号
    offset_ms     = Column(Integer, default=0)   # 段内偏移
    total_ms      = Column(Integer, default=0)   # 素材总时长
    finished      = Column(Boolean, default=False)
    updated_at    = Column(DateTime, default=_utcnow, onupdate=_utcnow)
```

**为什么进度放服务端**：设备会深度睡眠、会被换电池、固件会被重刷——**设备本地存不住可信状态**。放服务端则"换设备也能续播"。

详见 §11.11。

### 3.5 `material_audio` — 音频分段

```python
class MaterialAudio(Base):
    __tablename__ = "material_audio"
    id            = Column(String, primary_key=True, default=gen_id)
    material_id   = Column(String, index=True)
    segment_index = Column(Integer, default=0)   # 段号，从 0 开始
    path          = Column(String, default="")   # storage/materials/audio/<mid>/seg_003.mp3
    duration_ms   = Column(Integer, default=0)
    bytes         = Column(Integer, default=0)
    text          = Column(Text, default="")     # 该段对应的原文（供屏幕分行显示）
```

**`text` 字段是关键**：它让屏幕知道"当前该显示哪一段文字"，**文字与音频天然对齐**，不需要做时间轴同步。

详见 §11.11。


## 4. 三态与归档：为什么这样分

```
active    今天正在用的 1 条       → 全文 + 音频，供整天对话
recent    近 N 天（默认 30）      → 全文保留，供"回看/追问旧素材"
archive   超过 N 天               → 只留指纹，正文丢弃
```

### 归档的"压缩"到底压什么

| 动作 | 说明 |
|------|------|
| 1. 丢弃正文 | `body` 不进 archive（如需回看，从冷文件/按需重生成） |
| 2. 抽取指纹 | `concept_keys` + `fingerprint` + `one_line` |
| 3. 生成硬键 | `hard_key = domain + "|" + source + "|" + title`，唯一索引 |
| 4. 写排除清单 | 归档条目自动成为后续生成的负例素材 |

**关键数字**：一条归档指纹约 **100-200 字节**。一年 365 条约 **40-70KB** —— 这个体量**可以全量塞进 prompt** 作为"已用过清单"。

**这就是整个方案能成立的原因**：如果归档保留全文，一年后 context 根本放不下，模型只能看到最近几天，必然重复。降维到指纹后，"全年不重复"才在工程上可行。

## 5. 归档触发

复用项目已有的**夜间巡检**（`night_patrol_enabled`，默认 01:00-05:00，`routers/system.py` 已实现调度）：

```
夜间巡检追加一个任务：
  1. 查 materials where status='recent' and used_at < now - N天
  2. 逐条：抽指纹 → 写 archive → 删除或冷存正文 → status='archived'
  3. 校验 hard_key 唯一性（冲突则记录日志，不覆盖）
```

**为什么挂这里**：不新增调度器、不引入后台线程管理、天然避开用户活跃时段、且这个巡检已经稳定运行。同时保留手动触发入口 `POST /api/material/archive/run`（便于调试与补跑）。

## 6. 去重三层

这是"防止 AI 搞相同的"的正面实现。**三层缺一不可**：

### L1 硬键去重（底线，不依赖模型）

```python
hard_key = f"{domain}|{source}|{title}"
```
- 生成后先查 `material_archive.hard_key`（唯一索引）+ 近期 `materials`
- 命中 → 直接判重，**重生成，不落库**
- 覆盖场景：同一篇目、同一人物、同一概念被反复选中

### L2 关键词相似度（低成本语义判重）

- 取候选的 `concept_keys`，与 archive 中**近 90 天**的同 domain 条目算 **Jaccard 相似度**
- 阈值建议 **0.6**（可配），超阈值判重
- **纯 Python 实现，零新依赖**：概念关键词由模型在生成时一并产出，不需要分词库

### L3 prompt 负例（软约束，提升一次通过率）

生成时把**归档清单**作为负例注入：

```
已使用过的素材（禁止重复，也不要换皮重出同一概念）：
- [哲学] 苏格拉底·洞穴隐喻 | 概念：现象与本质, 认知局限
- [历史] 王安石变法·青苗法 | 概念：改革阻力, 制度成本
- [古诗文] 《劝学》· 宾语前置 | 概念：文言句式, 否定句代词前置
...
```

因为归档是降维的（§4），这个清单可以给到**全年**，不是只有最近几条。

### 重试与降级

```
生成 → L1 → L2 → 通过则落库
              └→ 不通过 → 携带"命中了哪几条"重生成（最多 3 次）
                            └→ 3 次仍失败：
                                a) 自动跳到 material_plan 下一条（推荐）
                                b) 回退到"请用户指定主题"
```

**降级优先选 a**：计划表存在就是为了这一刻——某条被反复判重，说明它和已有素材高度同质，直接换下一条比硬生成更合理，同时把该 plan 条目标 `note="同质，已跳过"`。

## 7. 每日素材生成流程（幂等）

**照抄 `daily_quote` 的已验证模式**：先查当天，有则直接返回，无则生成并落库。

```
GET /api/material/today
  │
  ├─ 1. 查 materials where day_key = 今天(北京时间) and status='active'
  │      └─ 命中 → 直接返回（含预生成音频 URL）。【这就是"开机若未准备新的则准备一个"】
  │
  ├─ 2. 未命中 → 取计划：material_plan
  │      ├─ 按 domain 轮换或加权取 status='pending' 且 seq 最小的一条
  │      └─ 标记为 "生成中"（防并发重复生成，用文件锁或 DB 状态）
  │
  ├─ 3. 检索原始资料：zhipuai_search / kimi_search
  │
  ├─ 4. 生成素材：deepseek_json（结构化输出 body/key_points/followups/concept_keys/fingerprint）
  │      注入 L3 负例清单（§6）
  │
  ├─ 5. 去重校验：L1 → L2（§6）
  │      ├─ 通过 → 落库，plan.status='used'
  │      └─ 不通过 → 重试 / 换下一条（§6 降级）
  │
  ├─ 6. 预生成音频：xiaomi_tts(body) → 存 storage/materials/audio/
  │
  └─ 7. 返回
```

**并发保护**：现有 `daily_quote` 用 `asyncio.Lock`，这里照做（`_material_lock`），避免设备开机连发两次请求造成双份生成。

## 8. 素材挂载式多轮对话

### 8.1 会话绑定素材

```
POST /api/material/chat  { session_id?, material_id, message }
```

- 会话对象持有 `material_id`（复用现有 `storage/sessions/*.json` 结构，加一个字段）
- 每次请求把素材的 `body` + `key_points` + `followups` 注入 system prompt
- **换素材 = 换 material_id，会话不中断**（这是"对话里要求查新的"能平滑工作的前提）
- 对话实现复用 `agent_multi_turn` 或 `deepseek_chat`，按是否要联网选模型

### 8.2 "查新的"用工具调用，不用关键词匹配

在 `_chat_with_tools` 里注册一个工具：

```json
{
  "name": "refresh_material",
  "description": "当用户要求换一个话题/素材，或当前素材已经聊透时调用",
  "parameters": { "domain": "philosophy|history|classics|any" }
}
```

**为什么不关键词匹配**："换个话题""这个聊完了""讲点别的""有没有别的"——这些表达无法穷举，关键词会漏。工具调用是模型语义判断，且项目已有基础设施。

触发后：走 §7 的生成流程（可指定 domain）→ 返回新 material_id → 会话切绑 → 向用户播报新素材开篇。

## 9. 计划表：把"顺序 & 大纲"数据化

用户在需求里明确要"提前拟定顺序 & 大纲——优先搜索哪些内容篇目和架构"。**这项需求在技术上就是 `material_plan` 表**，本次只定结构，内容后续填充。

### 取用策略（可配）

| 策略 | 说明 |
|------|------|
| `sequential` | 严格按 `seq` 顺序，一条一条走 |
| `round_robin` | 三个 domain 轮换（哲学 → 历史 → 古诗文 → 循环） |
| `weighted` | 按 domain 权重（如 古诗文 2 : 历史 1 : 哲学 1） |
| `adaptive` | 按 `feedback` 与 hippocampus 的掌握度调整 |

**建议默认 `round_robin`**：三个方向均衡覆盖，最不容易偏科，也最好预测。

### 反馈回写

`POST /api/material/feedback { material_id, vote: "up"|"down"|"skip", reason? }`
- `down` / `skip` → plan 条目标 `skipped` + 记 `note`，不再重复取用
- 热度信息可作为 `adaptive` 策略的输入

## 10. 接口设计

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/material/today` | 取今日素材（**幂等**，无则生成）；返回正文 + 音频 URL + 关键点 |
| POST | `/api/material/refresh` | 主动换素材，可指定 `domain` |
| POST | `/api/material/chat` | 挂载素材的多轮对话 |
| POST | `/api/material/feedback` | 评价/跳过，回写计划表 |
| GET | `/api/material/archive` | 归档清单（调试与人工核查去重效果） |
| POST | `/api/material/archive/run` | 手动触发归档（补跑/调试） |
| GET | `/api/material/plan` | 查看计划表状态 |
| POST | `/api/material/plan/import` | 批量导入大纲条目 |

**全部走现有全局 Bearer 鉴权**，不新增鉴权体系。

## 11. 精简设备：硬件支持性（2026-09-10 调研）

用户确认考察形式为**笔试**，设备定位为「很小巧，吃饭玩玩听听」。

### 11.1 结论：硬件不但支持，而且这条路比小智模式更轻

**ESP32 音频链路是整个 ESP32 生态里最成熟的部分，没有任何技术风险。**

乐鑫官方 `esp_player` 组件的实测性能（MP3 播放）：

| 芯片 | 格式 | 内存占用 | CPU 占用 | 启动延迟 |
|------|------|----------|----------|----------|
| **ESP32-S3** | MP3 | 100.6KB（内部 8.8 + PSRAM 91.8） | **5%** | **32ms** |
| ESP32-S3 | AAC | 116.5KB | 3% | 19ms |
| ESP32-P4 | MP3 | 99KB | 3% | 15ms |

**CPU 只占 5%、启动 32 毫秒** —— 播音频对 ESP32-S3 是轻负载，完全不构成瓶颈。

### 11.2 关键：我们的方案比小智实时对话简单一个量级

| | 小智实时对话模式 | **本方案模式** |
|---|---|---|
| 音频路径 | WebSocket 流式 + Opus 实时编解码 | **HTTP GET 下载预生成 MP3** |
| 服务器要求 | 必须实时推理、流式返回 | 无实时要求，音频已提前生成 |
| 设备职责 | 流式采集 + 流式播放 + 打断 | **下载 → 解码 → 播放** |
| 实现难度 | 高 | **低** |

因为素材音频是**服务端预生成**的（见 §0 决策 7），设备端**不需要 TTS 能力，也不需要流式协议**，只需要一个 HTTP 客户端加一个 MP3 解码播放器。

**这让"小巧、便宜、免动手"三件事同时成立。**

### 11.3 设备能力清单（v1）

```
必需：WiFi + 喇叭 + 按键 + 麦克风 + 双麦(带AEC) + 屏幕（需显示文字）+ 锂电
可选：墨水屏（长待机）、SD 卡（离线缓存音频与文字）、RTC
不需要：摄像头、触摸输入、TTS 合成、离线唤醒词
```

**屏幕是硬需求**（用户明确"不能没有，需要文字"），所以 §11.2 的「无屏」设想作废。但屏幕只需**显示**，不需要触摸——按键负责交互。

### 11.4 板卡候选（已按「不要小智 + 要屏 + 要长待机」重新筛选）

不用小智生态，就不受「小智官方支持板」约束，纯按硬件适配度选。

**A 组：墨水屏方案（推荐，对应长待机）**

| 方案 | 价格 | 屏 | 音频 | 锂电 | 备注 |
|------|------|----|------|------|------|
| **微雪 ESP32-S3-ePaper-3.97** ⭐ | 待查（多次检索到异常标价，疑缺货） | **3.97" 墨水屏 800×480，黑白双色** | **NS4150B 功放 + ES8311 编解码 + MX1.25 喇叭接口** | ✅ TG28 电源管理，3.7V MX1.25，含充电与电池管理 | ESP32-S3R8，**16MB Flash + 8MB PSRAM**，QMI8658 六轴 IMU，RTC，SHTC3，**拨轮按键（三方向）+ PWR + BOOT**，Micro SD |
| 微雪 ESP32-S3-ePaper-1.54G | ¥98.98（1+，4+ 降至 ¥93.83） | 1.54" 墨水屏 200×200，**四色（黑白黄红）** | 板载音频编解码芯片，支持语音采集与播放 | ✅ 充放电管理（分带/不带电池版本） | ESP32-S3，**8MB Flash + 8MB PSRAM**，RTC，SHTC3，**Micro SD 卡槽**；**全刷 20s / 快刷 15s**；官方定位「功耗极低、环境光下可视，适合长续航」 |
| 微雪 ESP32-S3 13.3" 全彩墨水屏 | — | 13.3"（E Ink Spectra 6 全彩） | 音频编解码芯片 | ✅ ETA6098 | ESP32-S3-WROOM-2-**N32R16V（32MB Flash + 16MB PSRAM）**；尺寸过大，不适合随身 |

**选墨水屏就必须选 3.97，不要选 1.54G。** 微雪官方文档把两者分得很清楚：

- **双色（黑白）→ ESP32-S3-ePaper-3.97**
- **四色（黑白黄红）→ ESP32-S3-ePaper-1.54G**

**1.54G 全刷 20s / 快刷 15s 的根源就是它四色**。而我们的场景每条对话回答都要更新文字，15 秒一次完全不可用。**黑白双色的 3.97 才是可行解。**

另外注意微雪对墨水屏的官方定性：**「刷新以秒计，不适合动画」**——所以 §11.11 的「一段一刷」设计不是将就，而是必须。

**B 组：IPS 方案（备选，屏幕刷新快但不长待机）**

| 方案 | 价格 | 屏 | 音频 | 备注 |
|------|------|----|------|------|
| 微雪 ESP32-S3-Touch-LCD-1.54 | — | 1.54" IPS 240×240 | 双麦 | 定制方形塑料外壳（带壳是优点）；PSRAM 8MB |
| 微雪 ESP32-S3-AUDIO-Board | $15.99-17.99（约 ¥115-130） | 无屏（可外接） | **双麦阵列，支持降噪与回声消除** | 环绕 RGB 灯；**无屏，不满足"需要文字"** |

**排除项**

| 方案 | 排除理由 |
|------|----------|
| ~~Xmini-C3（¥58）~~ | 小智官方设计，按「不要小智」排除；且单麦无 AEC |
| ~~自焊套件（¥38 起）~~ | INMP441 单麦、无 AEC，外放场景不可用 |

**内存门槛**：Flash ≥ 16MB、PSRAM ≥ 4MB。自研固件虽不受小智限制，但 MP3 流式播放 + 录音并发时这两项仍是实际底线。**ePaper-1.54G 是 8MB Flash + 8MB PSRAM，Flash 略低于该线，需实测确认是否够用。**

### 11.5 AEC 从「加分」变回「刚需」（重要修正）

之前判断 AEC 不重要，**前提是戴耳机**。现在明确**外放**，结论反转：

外放 + 麦克风同时工作 → 喇叭放出的声音被麦克风收进去 → 设备听见自己说话 → **误唤醒、自问自答、打断自己的回答**。

**这必须靠硬件回声消除解决，软件补不回来。** 所以：

- 双麦阵列 + 硬件 AEC，**从「加分项」升级为「必需项」**
- 单麦方案（INMP441 面包板套件、Xmini-C3）在这个场景下**直接不可用**
- 微雪 AUDIO-Board 的双麦方案正是为此设计

### 11.6 TTS 双路：设备端复杂度不变

聊天需要实时 TTS。已核实服务端契约（`backend/routers/audio.py` + `ai_service.xiaomi_tts`）：

| 用途 | 音频来源 | 格式 | 设备端动作 |
|------|----------|------|-----------|
| 素材讲解 | **预生成**，服务端缓存 | **MP3**（默认） | HTTP GET → 解码 → 播放 |
| 聊天回答 | **实时**调 MiMo TTS | 可选 `wav` / `mp3` / `pcm` | **同上，代码完全一样** |

**关键结论：两条路对设备的要求完全相同**——都是「HTTP 拉一段音频字节 → 解码 → I2S 输出」。差别只在服务端（读缓存 vs 实时合成），**设备端不需要增加任何能力**。

格式取舍：

- **MP3**：体积小，适合长素材；需 ESP-ADF / `esp_player` 解码（实测 CPU 仅 5%）
- **WAV / PCM**：设备端**完全不需要解码器**，字节直接喂 I2S，开发最省事；代价是体积大（16kHz/16bit 单声道约 32KB/秒）

**建议 v1 用 MP3**（服务端零改动、组件现成）；若嫌 ESP-ADF 太重，再切 WAV/PCM。

**必须设计的体验细节**：实时 TTS 链路是「说话 → 上传 → ASR → LLM → TTS → 回传」，约 **3-8 秒延迟**。设备需要一个「正在思考」的状态提示：墨水屏刷一行"思考中…"，或板载 RGB / 单色 LED 变色。

**v2 优化方向**：边生成边播（LLM 出第一句就 TTS 第一句），首句延迟可压到 1-2 秒。v1 整段等，不承诺。

### 11.7 形态建议

**按键交互 + 墨水屏显示文字 + 外放 + 掌心大小**

- 墨水屏待机时**断电保持显示**：合上后屏幕上还留着上次读到的原文和进度，随时一瞥即可（这是 IPS 给不了的）
- 交互全交给按键：**播放/暂停、上一段/下一段、换素材、开始说话**（ePaper-3.97 自带三方向拨轮，正好对应）
- 不做触摸（墨水屏触摸成本高且没必要）
- 既然自研固件，本来也不带唤醒词，按键即可

### 11.8 设备侧接口（只消费四个）

```
开机   → GET  /api/material/today          → 拉素材元数据 + 文字 + 首段音频
续播   → GET  /api/material/progress       → 取上次断点，从该段继续
上报   → POST /api/material/progress       → 每段播完上报一次（也用于关机前保存）
换素材 → POST /api/material/refresh        → 换一条素材
提问   → POST /api/material/chat + /api/tts（实时）→ 录音上传，取回音频播放
```

### 11.9 待确认

| # | 事项 | 影响 |
|---|------|------|
| 1 | ePaper-3.97 的实际到手价格（多次检索到异常标价，疑缺货） | 决定预算；**当前最大未知项** |
| 2 | **ePaper-3.97（黑白双色）的确切刷新时间与局刷能力** | 已确认它是双色屏（快于四色的 1.54G），但具体秒数需从 [微雪 wiki](https://docs.waveshare.net/ESP32-Peripheral-Tutorials/Display/E-Paper/) 核实 |
| 3 | ePaper-3.97 的麦克风是单麦还是双麦、有无 AEC | **决定外放场景能否成立**（§11.5） |
| 4 | 微雪 ePaper 系列是否提供配套外壳 | 决定是否需要碰 3D 打印 |
| 5 | 3.97 的 16MB Flash + 8MB PSRAM 是否够（固件 + MP3 解码 + 中文点阵字库） | 中文点阵字库体积不可忽略，需实测 |

### 11.10 为什么倾向墨水屏：三个约束恰好交汇

用户的三条约束——**要屏、要文字、要长待机**——单独看都能满足，凑在一起只有墨水屏能同时接住：

| 约束 | 墨水屏 | IPS |
|------|--------|-----|
| 待机时屏幕保持显示 | **✅ 断电保持，零功耗** | ❌ 必须关闭，否则耗电 |
| 静态功耗 | **✅ 仅在刷新时耗电** | ❌ 常亮持续耗电 |
| 环境光可读（食堂、教室） | **✅ 反射式，不刺眼** | ❌ 强光下看不清 |
| 文字阅读体验 | **✅ 类纸，适合长文** | ⚠️ 发光，久看累眼 |
| 刷新速度 | **❌ 慢（秒级到 20s）** | ✅ 流畅 |
| 播放进度条 / 实时滚动 | **❌ 不可行** | ✅ 可以 |
| 暗处使用 | **❌ 无背光** | ✅ 有 |

**关键推论：墨水屏还有一个杀手特性——断电后保持显示。** 对「吃饭时随手看看」这个场景，合上设备后屏幕上仍留着上次读到的段落和进度，**开机即见，不需要刷新、不需要背光、不消耗电**。这正是 §11.7 形态建议的基础。

**代价要讲清楚**：墨水屏刷新慢，**做不了进度条和实时滚动**。但我们的交互恰好是「一段一段播、一段一段刷」，天然错开了这个短板——见 §11.11。

另外，用户此前那份 [学校迷你板方案](20260910-Mavis-学校迷你板方案.md) 就是墨水屏方向。**两个需求在这里合流了**：自招素材终端可以直接长在那份方案的技术选型上，而不是另起一摊。

### 11.11 断点续播：分段播放 + 服务端记进度

长素材（几千字讲解，可能 5-15 分钟）不可能一次听完，必须支持续播。

**方案：把音频按语义切成段，进度 = 段号 + 段内偏移。**

```
素材生成时：
  正文按段落/TTS 自然停顿切分 → 每段单独 TTS → seg_000.mp3, seg_001.mp3 ...
  同时记录每段对应的原文 text（写进 material_audio.text）

播放时：
  设备拉取 seg_N.mp3 → 播放 → 播完上报 progress(segment_index=N)
  关机/中断时上报 progress(segment_index=N, offset_ms=X)
  下次开机拉 progress → 从 seg_N 的 X 位置继续
```

**为什么分段比 HTTP Range 好**

| | 分段方案 | 单文件 + HTTP Range |
|---|---|---|
| 断点精度 | 段级 + 段内偏移，精确 | 字节级，但 **MP3 按字节切会踩碎帧边界**，需额外做帧同步 |
| 首播延迟 | **播完第一段就出声**（可边下边播） | 需先缓冲一段才敢播 |
| 网络抖动 | 单段小，重试代价低 | 单文件大，断了要重连 |
| 与屏幕配合 | **段号即页码**，屏幕显示"第 3/8 段" | 需自己算时间戳映射 |

**和墨水屏的完美配合**：墨水屏刷新慢、做不了进度条，但我们的进度表达是**文字**——「第 3/8 段 · 已播 2:15」。**每段刷新一次屏**（十几秒一次），恰好落在墨水屏能承受的刷新频率内。

**和归档机制的配合**：`material_progress.finished=True` 是"这条素材听完了"的信号，可作为归档与反馈的输入（听完的才进 archive 指纹，没听完的下次优先继续）。

**设备会深度睡眠**（长待机要求），本地存不住可信状态，所以进度放服务端（§3.4）——顺带的好处是**换设备也能续播**。


## 12. 开关与配置

沿用项目 `config.py` 的 `ENABLE_*` 约定：

```python
ENABLE_MATERIAL_SYSTEM = True    # 自招素材系统总开关
ENABLE_MATERIAL_AUDIO  = True    # 素材音频预生成（依赖小爱 TTS）
```

`settings.json` 新增（均可留空走默认）：

```json
{
  "material_recent_days": 30,        // 全文保留天数，超过则归档
  "material_dedup_threshold": 0.6,   // L2 Jaccard 阈值
  "material_pick_strategy": "round_robin",
  "material_generate_timeout": 120,
  "material_domains": ["philosophy", "history", "classics"]
}
```

## 13. 分期

| 阶段 | 内容 | 验收 |
|------|------|------|
| **P1** | `materials` 表 + `GET /api/material/today` 幂等生成（先不做归档/去重） | 今天请求生成，当天重复请求返回同一条 |
| **P2** | `material_archive` + 归档降维 + 挂夜间巡检 | 昨天的素材次日变成指纹条目，正文已释放 |
| **P3** | 去重三层（L1 硬键 → L2 Jaccard → L3 负例） | 连续生成 10 天，硬键零重复；`concept_keys` 相似度不超阈值 |
| **P4** | 对话挂载 + `refresh_material` 工具调用 | 对话中"换个话题"能正确切换素材且不断会话 |
| **P5** | 音频预生成 + **按段切分 + 每段文字对齐** + `material_plan` 表与导入 | 开机即播；屏幕每段刷新一次恰好落在墨水屏刷新能力内 |
| **P6** | 反馈回写 + adaptive 策略 + `material_progress` 断点续播 | `down` 的条目不再出现；中断后能从原段续播 |
| **P7** | 设备固件（自研 ESP-IDF：WiFi / HTTP / MP3 解码 / I2S 播放 / 录音上传 / 墨水屏驱动 / 按键） | 另开固件设计文档 |

**P1-P3 是核心**：只要这三步成立，"每天一条、不重复、可归档"这个系统的骨架就立住了，后面都是增强。

## 14. 风险与待决

### 风险

| # | 风险 | 应对 |
|---|------|------|
| 1 | 去重只靠模型约束 → 必然漏 | **L1 硬键是底线**，不依赖模型；L2 纯算法兜底 |
| 2 | 素材方向跑偏 | `material_plan` 把选题权收回，模型只做"展开"不做"选题" |
| 3 | 归档后无法回看 | 正文**冷存到文件**而非彻底删除，保留按需取回通道（若确实不需要回看，可改为真删） |
| 4 | 生成失败导致当天无素材 | 降级：跳到计划表下一条；再失败则返回上一条作为"昨日回顾"，不留空 |
| 5 | 音频预生成失败 | 不阻塞素材返回，`audio_path` 留空，设备端回退到实时 TTS |
| 6 | 来源可靠性 | 联网搜索内容需标注来源；哲学/历史解读属二次加工，有版权与准确性问题，`source` 字段强制填写 |

### 待决（需要用户输入）

| # | 问题 | 影响 |
|---|------|------|
| 1 | ~~考察形式：面试还是笔试~~ | **已确认：笔试**（2026-09-10）。素材偏「知识点」侧，字段以**篇目 / 句法 / 史实 / 概念**为主，`fingerprint` 按 `{works, syntax, persons, era, concepts}` 组织。若实际还含面试环节，再补「观点/可辩论点」字段 |
| 2 | 三个 domain 的比例与轮换策略 | 默认 `round_robin`，可改 `weighted` |
| 3 | `recent` 全文保留多少天 | 默认 30 天 |
| 4 | 归档正文是**冷存可回看**还是**真删** | 影响存储占用与"回看旧素材"功能是否成立 |
| 5 | 计划表内容何时填充 | 本次按用户要求后置 |
| 6 | 是否复用 `SavedConfig` 而非新建 `material_plan` 表 | 表更清晰，但多一张表；倾向新建 |
| 7 | ~~设备是否需要耳机输出~~ | **已确认外放可以**（2026-09-10），无需耳机口。但由此**双麦 AEC 成为必需**，见 §11.5 |
| 8 | ~~设备是否要离线唤醒词~~ | **不用小智生态，自研固件，本来也不带唤醒词**（2026-09-10）。按键唤醒是最终形态（§11.7） |
| 9 | 墨水屏刷新时间、麦克风 AEC、外壳、价格等设备侧问题 | **统一收敛到 §11.9 的 5 项待确认**，不在此重复 |
| 10 | 音频分段粒度（按段落 / 按固定时长） | 影响屏幕"翻页"手感与断点精度（§11.11） |
| 11 | 自研固件的 MP3 播放走 ESP-ADF 还是轻量 `esp_player` 组件 | 影响固件体积与开发量 |
| 12 | 墨水屏的中文字库如何取舍（全字库体积大，需裁剪或按素材动态取字） | 影响 Flash 占用，是嵌入式中文显示的经典坑 |
